schema_version: looper.standard-pressure-protocol/v1alpha1
id: cpu-stress-ng-governor-aliyun-hard-gate-20260823
component: cpu
target_scope: Alibaba ECS iZ7xvcxcry1ejfjcd1d7igZ after the tuned handover, all eight online vCPUs, fixed stress-ng matrixprod method under a uniform governor.
limitation: Target-local exploratory governor search under the post-handover schedutil baseline; not portable to Tencent Cloud CVM.
required_executables: [python3, /opt/looper-system-opt-b9e2ae5/.venv/bin/python]
input_identity:
  policy_id: aliyun-ecs-cpu-governor-20260823
  evidence_kind: empirical-target-local
  stability_calibration_digest: sha256:c2f57638f436a677176589e694b9ae7365dabb35c34bb021daf0d021e608790c
metric_ids: [cpu.bogo-ops-per-second, cpu.success]
gate_metric_ids: [cpu.success]
stability:
  metric_id: cpu.bogo-ops-per-second
  statistic: cv
  enforcement: hard-gate
  acceptance_limit: 0.0031146792925256066
  minimum_repeats: 7
  maximum_repeats: 7
  source: Target-local one-sided 95-percent bootstrap upper CV from seven frozen schedutil-baseline samples, 2000 resamples, seed 20260823.
phases:
  - id: exclusive-window
    kind: prepare
    command:
      argv: [/opt/looper-system-opt-b9e2ae5/.venv/bin/python, -m, looper_core.system_opt.interference, --forbid-regex, "Phoronix Test Suite", --forbid-regex, phpbench, --forbid-regex, "benchmark-packages/.*/producer.py", --forbid-regex, stress-ng, --forbid-regex, sysbench, --forbid-regex, "(^|/)fio( |$)", --forbid-regex, iperf3, --forbid-regex, pytest, --output, .artifacts/system-opt/m2-cpu-governor-search-20260823/interference-preflight.json]
      timeout_seconds: 15
    declared_duration_seconds: 0
    purpose: Fail before warmup when another benchmark or pressure tool owns the shared target window; raw command lines are not retained.
  - id: prepare
    kind: prepare
    command:
      argv: [python3, examples/system-optimizer/cpu_pressure_measure.py, prepare, --output-dir, .artifacts/system-opt/m2-cpu-governor-search-20260823, --repeats, "{repeats}", --workers, "8", --method, matrixprod, --duration-seconds, "5"]
      timeout_seconds: 15
    declared_duration_seconds: 0
    purpose: Verify Linux, stress-ng, online CPU count, and frozen workload inputs under the current uniform governor.
  - id: warmup
    kind: warmup
    command:
      argv: [python3, examples/system-optimizer/cpu_pressure_measure.py, warmup, --output-dir, .artifacts/system-opt/m2-cpu-governor-search-20260823, --repeats, "{repeats}", --workers, "8", --method, matrixprod, --duration-seconds, "5"]
      timeout_seconds: 45
    declared_duration_seconds: 5
    purpose: Discard one complete full-load warmup interval.
  - id: measure
    kind: measure
    command:
      argv: [python3, examples/system-optimizer/cpu_pressure_measure.py, measure, --output-dir, .artifacts/system-opt/m2-cpu-governor-search-20260823, --repeats, "{repeats}", --workers, "8", --method, matrixprod, --duration-seconds, "5"]
      timeout_seconds: 120
    declared_duration_seconds: 35
    purpose: Emit seven fixed-method CPU throughput repetitions as one MeasurementBatch.
  - id: exclusive-window-after
    kind: verify
    command:
      argv: [/opt/looper-system-opt-b9e2ae5/.venv/bin/python, -m, looper_core.system_opt.interference, --forbid-regex, "Phoronix Test Suite", --forbid-regex, phpbench, --forbid-regex, "benchmark-packages/.*/producer.py", --forbid-regex, stress-ng, --forbid-regex, sysbench, --forbid-regex, "(^|/)fio( |$)", --forbid-regex, iperf3, --forbid-regex, pytest, --output, .artifacts/system-opt/m2-cpu-governor-search-20260823/interference-postflight.json]
      timeout_seconds: 15
    declared_duration_seconds: 0
    purpose: Invalidate the batch if another benchmark or pressure tool entered the shared target during measurement.
  - id: verify
    kind: verify
    command:
      argv: [python3, examples/system-optimizer/cpu_pressure_measure.py, verify, --output-dir, .artifacts/system-opt/m2-cpu-governor-search-20260823, --repeats, "{repeats}", --workers, "8", --method, matrixprod, --duration-seconds, "5"]
      timeout_seconds: 15
    declared_duration_seconds: 0
    purpose: Require the expected sample count shape, finite positive values, and success gate.
  - id: cleanup
    kind: cleanup
    command:
      argv: [python3, examples/system-optimizer/cpu_pressure_measure.py, cleanup, --output-dir, .artifacts/system-opt/m2-cpu-governor-search-20260823, --repeats, "{repeats}", --workers, "8", --method, matrixprod, --duration-seconds, "5"]
      timeout_seconds: 15
    declared_duration_seconds: 0
    purpose: Fail if a pressure-process marker remains.
